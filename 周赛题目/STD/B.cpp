#include <bits/stdc++.h>
using namespace std;
#define int long long
void Sol () {
    int N, K; cin >> N >> K;
    
}
signed main () {
    ios::sync_with_stdio (false);
    cin.tie (nullptr);
    // int _; cin >> _;
    // while (_ -- )
        Sol ();
}
